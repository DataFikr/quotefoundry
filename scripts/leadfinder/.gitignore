.venv/
__pycache__/
*.pyc
.env
companies.csv
*.csv

# Scraped lead data = third-party business PII. Keep OUT of the (public) repo.
# The scripts (parse/score/send) are code and ARE committed; the data is not.
shopList*.json
thomasnet*.txt
*_leads.txt
